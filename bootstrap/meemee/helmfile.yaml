---
# yaml-language-server: $schema=https://json.schemastore.org/helmfile

helmDefaults:
  cleanupOnFail: true
  wait: true
  waitForJobs: true
  # waitRetries: 3 # Not supported by Helm yet

releases:
  - name: cilium
    namespace: kube-system
    atomic: true
    chart: '{{ requiredEnv "ROOT_DIR" }}/kubernetes/apps/kube-system/cilium'
    values:
      # meemee is ran on VMs, native routing will fail miserably,
      # so let cilium tunnel everything
      - cilium:
          autoDirectNodeRoutes: false
          bgpControlPlane:
            enabled: false
          cluster:
            name: meemee
          enableIPv4BIGTCP: false
          ipv4NativeRoutingCIDR: ""
          routingMode: tunnel
          tunnelProtocol: geneve
        pools:
          - 10.100.31.0/24
    hooks:
      # Wait for cilium CRDs to be available
      - events: ['postsync']
        command: bash
        args:
          - -c
          - >-
            until kubectl get crd ciliumbgppeeringpolicies.cilium.io ciliuml2announcementpolicies.cilium.io
            ciliumloadbalancerippools.cilium.io ciliumclusterwidenetworkpolicies.cilium.io &>/dev/null;
            do sleep 10; done
        showlogs: true

  - name: coredns
    namespace: kube-system
    atomic: true
    chart: '{{ requiredEnv "ROOT_DIR" }}/kubernetes/apps/kube-system/coredns'
    needs: ['kube-system/cilium']

  - name: spegel
    namespace: kube-system
    atomic: true
    chart: '{{ requiredEnv "ROOT_DIR" }}/kubernetes/apps/kube-system/spegel'
    needs: ['kube-system/coredns']

  # - name: argocd
  #   namespace: argocd
  #   atomic: true
  #   chart: oci://ghcr.io/argoproj/argo-helm/argo-cd
  #   version: 7.8.15
  #   values: ['{{ requiredEnv "ROOT_DIR" }}/kubernetes/apps/argocd/argocd/values.yaml']
  #   needs: ['kube-system/spegel']
