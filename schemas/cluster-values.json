{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "homelab-values",
  "type": "object",
  "properties": {
    "applications": {
      "type": "object",
      "patternProperties": {
        "^[a-z-]+$": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "namespace": {
              "description": "The namespace to which application will be deployed.",
              "maxLength": 63,
              "minLength": 1,
              "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
              "type": "string"
            },
            "path": {
              "description": "Path, relative to root directory of repository, where helm template is stored.",
              "pattern": "^(?:[a-z0-9-]+/)+(?:[a-z0-9-]+)$",
              "type": "string"
            },
            "psp": {
              "description": "Pod security policy for given namespace.",
              "enum": [
                "restricted",
                "baseline",
                "privileged"
              ],
              "type": "string"
            },
            "ignoreDifferences": {
              "description": "IgnoreDifferences is a list of resources and their fields which should be ignored during comparison. Will be passed directly to application.",
              "type": "string"
            },
            "helmValues": {
              "description": "Values specifies Helm values to be passed to helm template, typically defined as a block. Will be passed directly to application.",
              "type": "string"
            },
            "syncOptions": {
              "description": "Options allow you to specify whole app sync-options. 'CreateNamespace=true' is passed by default.",
              "items": {
                "type": "string"
              },
              "type": "array"
            }
          },
          "required": [
            "namespace",
            "path"
          ]
        }
      }
    }
  }
}
